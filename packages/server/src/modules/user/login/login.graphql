# type LoginResponse {
#   errors: [Error!]
#   sessionId: String
# }

type Mutation {
  # login(email: String!, password: String!): LoginResponse!
  login(email: String!, password: String!): [Error!]
}
