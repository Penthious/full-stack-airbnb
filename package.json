{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "docker:build": "sudo docker build -t penthious/airbnb:1.0.0 .",
        "start": "yarn docker:build && sudo docker-compose up"
    }
}