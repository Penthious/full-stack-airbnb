{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "docker:build": "sudo docker build -t penthious/airbnb-clone:1.0.0 .",
        "start": "yarn docker:build && sudo docker-compose up"
    },
    "devDependencies": {
        "lerna": "^2.11.0"
    }
}
